import { AppComponent } from './app.component';

describe('AppComponent', () => {
	it('should create the AppComponent without errors', () => {
		const appComponent = new AppComponent();
		expect(appComponent).toBeTruthy();
	});
});
